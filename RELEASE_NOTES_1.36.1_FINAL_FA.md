# یادداشت انتشار Sokna 1.36.1

`1.36.1` نسخه نهایی اصلاحی روی نسخه منتشرشده `1.36.0` است. این نسخه تمام اصلاحات `1.36.1-rc.1` و اصلاحات تأییدشده پس از آن را یکجا دارد و Migration دیتابیس ندارد.

## اصلاحات اصلی

- منوهای سه‌نقطه و Action Menuهای ردیفی به Portal سطح `body` منتقل شدند و Swipe-down شیت موبایل به Owner مشترک متصل شد.
- هندسه استپر Quick Order یکپارچه شد؛ در ردیف‌های دارای دو استپر، اطلاعات قلم تمام‌عرض و کنترل‌ها در ردیف جدا نمایش داده می‌شوند.
- URL دارایی‌های CSS، JavaScript و SVG دارای نسخه و اثرانگشت محتوا است تا Cache قدیمی جای فایل جدید را نگیرد.
- سیستم آیکن یکپارچه شد و به‌روزرسانی متن پویا دیگر آیکن گزینه اعلان را حذف نمی‌کند.
- رابط «درخواست خرید»، حذف با سطل زباله، نوار ثبت و فهرست انتظار خرید در Mobile/Desktop اصلاح شد.
- اقدام سریع Notification فراخوان گارسون به «رسیدگی شد» تبدیل شد و فراخوان را اتمیک تکمیل و از فهرست باز حذف می‌کند.
- عرض صفحات چاپ و طراحی قالب روی Desktop به حداکثر ۱۱۲۰ پیکسل محدود شد.

## مسیر ارتقا

`1.36.0 → 1.36.1` یا `1.36.1-rc.1 → 1.36.1` از مسیر `/admin/update/` با Updater Engine `1.5.3` و بدون Migration دیتابیس.

## محدودیت پذیرش

گیت‌های استاتیک، Syntax جاوااسکریپت، قرارداد Manifest، Hash/Size و هم‌ارزی بسته در محیط ساخت بررسی می‌شوند. اجرای واقعی PHP، MariaDB/MySQL، Browser، گوشی، Service Worker و چاپگر در محیط ساخت حاضر نیست و `UAT_REQUIRED` باقی می‌ماند.
