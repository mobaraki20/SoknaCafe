# یادداشت انتشار Sokna 1.36.1-rc.1

`1.36.1-rc.1` یک RC اصلاحی روی نسخه منتشرشده `1.36.0` است. این نسخه مشکلات گزارش‌شده رابط را در Ownerهای اصلی Component و Cache حل می‌کند و Migration دیتابیس ندارد.

## اصلاحات اصلی

- منوهای سه‌نقطه سراسری و ردیفی هنگام بازشدن به `body` Portal می‌شوند تا Backdrop تار بدون Sheet قابل‌دیدن ایجاد نشود.
- استپر Quick Order یک هندسه واحد سه‌خانه ۴۴ پیکسلی دارد و آیکن‌های `+` و `−` از Sprite مشترک با اندازه نوری ثابت می‌آیند.
- URL آیکن‌ها با نسخه و Digest محتوای Asset تولید می‌شود تا Service Worker یا Cache مرورگر نسخه قدیمی Sprite را نگه ندارد.
- Glyphهای دستی باقی‌مانده حذف و انتخاب‌گر آیکن دسته‌بندی با Tile و وزن خط همسان بازطراحی شد.
- واژه رابط «درخواست خرید» یکپارچه شد، حذف ردیف با سطل زباله است، نوار ثبت روی ورودی‌ها نمی‌افتد و فهرست انتظار خرید Responsive است.
- عرض Workspace چاپ و طراحی قالب روی Desktop حداکثر ۱۱۲۰ پیکسل شد.

## مسیر ارتقا

`1.36.0 → 1.36.1-rc.1` از مسیر `/admin/update/` با Updater Engine `1.5.3` و بدون Migration دیتابیس.

## محدودیت پذیرش

گیت‌های استاتیک و Syntax قابل‌اجرا، قرارداد Manifest، Hash/Size و هم‌ارزی بسته بررسی می‌شوند. PHP، MariaDB/MySQL، Browser واقعی، گوشی واقعی و چاپگر در محیط ساخت حاضر نیستند؛ این موارد `UAT_REQUIRED` باقی می‌مانند و انتشار Artifact به‌معنای استقرار Production نیست.
