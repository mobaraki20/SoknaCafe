# فونت ثابت سامانه

Sokna فقط با خانواده **Vazirmatn** نمایش داده می‌شود. فایل رسمی نسخه `v33.003` داخل پروژه و با نام زیر نگهداری می‌شود:

`assets/fonts/Vazirmatn-Variable.woff2`

SHA-256 فایل رسمی:

`4e3fa217d38fdafc1fea4414ceb58ca5e662cf0ab5fa735a8c8c20e8b42cad92`

مکانیزم Runtime فقط در صورت حذف یا خراب‌شدن فایل، همان نسخه قفل‌شده را از مخزن رسمی بازیابی می‌کند. متن کامل مجوز SIL OFL 1.1 در `assets/fonts/OFL.txt` قرار دارد.
